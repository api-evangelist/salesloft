openapi: 3.0.0
info:
  title: Salesloft Account and People Redaction Bulk Jobs - Job Data API
  description: This data allows integration owners to self-serve and claim more ownership over API usage, especially when using the Signals API to integrate with Rhythm. This ...
  version: 1.0.0
servers:
- url: http://{{salesloft_api_base_url}}
- url: https://accounts.salesloft.com
- url: http://{{salesloft_api_base_url}}external
security:
- oauth2Auth: []
tags:
- name: Bulk Jobs - Job Data
  description: '[https://developer.salesloft.com/docs/api/bulk-jobs-job-data/](https://developer.salesloft.com/docs/api/bulk-jobs-job-data/)'
paths:
  /bulk_jobs/{bulk_jobs_id}/job_data/{bulk_jobs_id}:
    post:
      tags:
      - Bulk Jobs - Job Data
      summary: Salesloft Create Job Data for a Bulk Job
      requestBody:
        content:
          application/json:
            schema:
              type: object
              example:
                data: []
      parameters:
      - name: bulk_jobs_id
        in: path
        schema:
          type: string
        required: true
      - name: bulk_jobs_id
        in: path
        schema:
          type: string
        required: true
      responses:
        '200':
          description: Successful response
          content:
            application/json: {}
    get:
      tags:
      - Bulk Jobs - Job Data
      summary: Salesloft List Job Data for a Bulk Job
      parameters:
      - name: status
        in: query
        schema:
          type: string
      - name: id
        in: query
        schema:
          type: string
      - name: per_page
        in: query
        schema:
          type: string
      - name: bulk_jobs_id
        in: path
        schema:
          type: string
        required: true
      - name: bulk_jobs_id
        in: path
        schema:
          type: string
        required: true
      responses:
        '200':
          description: Successful response
          content:
            application/json: {}
components:
  securitySchemes:
    noauthAuth:
      type: http
      scheme: noauth
    bearerAuth:
      type: http
      scheme: bearer
    oauth2Auth:
      type: http
      scheme: oauth2
