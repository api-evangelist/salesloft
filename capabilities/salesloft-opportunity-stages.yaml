naftiko: 1.0.0-alpha2
info:
  label: Salesloft — Opportunity Stages
  description: 'Salesloft — Opportunity Stages. 5 operations. Lead operation: Salesloft List Opportunity Stages. Self-contained
    Naftiko capability covering one Salesloft business surface.'
  tags:
  - Salesloft
  - Opportunity Stages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESLOFT_API_KEY: SALESLOFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesloft-opportunity-stages
    baseUri: http://{{salesloft_api_base_url}}
    description: Salesloft — Opportunity Stages business capability. Self-contained, no shared references.
    resources:
    - name: opportunity_stages
      path: /opportunity_stages
      operations:
      - name: get
        method: GET
        description: Salesloft List Opportunity Stages
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: Salesloft Create an Opportunity Stage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: opportunity_stages-id
      path: /opportunity_stages/{id}
      operations:
      - name: get
        method: GET
        description: Salesloft Fetch an Opportunity Stage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          required: true
      - name: put
        method: PUT
        description: Salesloft Update an Opportunity Stage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: delete
        method: DELETE
        description: Salesloft Delete an Opportunity Stage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESLOFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesloft-opportunity-stages-rest
    port: 8080
    description: REST adapter for Salesloft — Opportunity Stages. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/opportunity-stages
      name: opportunity-stages
      description: REST surface for opportunity_stages.
      operations:
      - method: GET
        name: get
        description: Salesloft List Opportunity Stages
        call: salesloft-opportunity-stages.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Salesloft Create an Opportunity Stage
        call: salesloft-opportunity-stages.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/opportunity-stages/{id}
      name: opportunity-stages-id
      description: REST surface for opportunity_stages-id.
      operations:
      - method: GET
        name: get
        description: Salesloft Fetch an Opportunity Stage
        call: salesloft-opportunity-stages.get
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: put
        description: Salesloft Update an Opportunity Stage
        call: salesloft-opportunity-stages.put
        with:
          id: rest.id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Salesloft Delete an Opportunity Stage
        call: salesloft-opportunity-stages.delete
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesloft-opportunity-stages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesloft — Opportunity Stages. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: salesloft-list-opportunity-stages
      description: Salesloft List Opportunity Stages
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-opportunity-stages.get
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-create-opportunity-stage
      description: Salesloft Create an Opportunity Stage
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesloft-opportunity-stages.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-fetch-opportunity-stage
      description: Salesloft Fetch an Opportunity Stage
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-opportunity-stages.get
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-update-opportunity-stage
      description: Salesloft Update an Opportunity Stage
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: salesloft-opportunity-stages.put
      with:
        id: tools.id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-delete-opportunity-stage
      description: Salesloft Delete an Opportunity Stage
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: salesloft-opportunity-stages.delete
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
