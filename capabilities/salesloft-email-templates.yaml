naftiko: 1.0.0-alpha2
info:
  label: Salesloft — Email Templates
  description: 'Salesloft — Email Templates. 4 operations. Lead operation: Salesloft List Email Templates. Self-contained
    Naftiko capability covering one Salesloft business surface.'
  tags:
  - Salesloft
  - Email Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESLOFT_API_KEY: SALESLOFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesloft-email-templates
    baseUri: http://{{salesloft_api_base_url}}
    description: Salesloft — Email Templates business capability. Self-contained, no shared references.
    resources:
    - name: email_templates
      path: /email_templates
      operations:
      - name: get
        method: GET
        description: Salesloft List Email Templates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ids
          in: query
          type: string
        - name: updated_at[gt]
          in: query
          type: string
        - name: updated_at[gte]
          in: query
          type: string
        - name: updated_at[lt]
          in: query
          type: string
        - name: updated_at[lte]
          in: query
          type: string
        - name: linked_to_team_template
          in: query
          type: string
        - name: search
          in: query
          type: string
        - name: tag_ids
          in: query
          type: string
        - name: tag
          in: query
          type: string
        - name: filter_by_owner
          in: query
          type: string
        - name: group_id
          in: query
          type: string
        - name: include_cadence_templates
          in: query
          type: string
        - name: include_archived_templates
          in: query
          type: string
        - name: cadence_id
          in: query
          type: string
        - name: sort_by
          in: query
          type: string
        - name: sort_direction
          in: query
          type: string
        - name: per_page
          in: query
          type: string
        - name: page
          in: query
          type: string
        - name: include_paging_counts
          in: query
          type: string
        - name: limit_paging_counts
          in: query
          type: string
      - name: post
        method: POST
        description: Salesloft Create Personal Email Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: email_templates-id
      path: /email_templates/{id}
      operations:
      - name: put
        method: PUT
        description: Salesloft Update Personal Email Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: get
        method: GET
        description: Salesloft Fetch an Email Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: include_signature
          in: query
          type: string
        - name: id
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESLOFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesloft-email-templates-rest
    port: 8080
    description: REST adapter for Salesloft — Email Templates. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/email-templates
      name: email-templates
      description: REST surface for email_templates.
      operations:
      - method: GET
        name: get
        description: Salesloft List Email Templates
        call: salesloft-email-templates.get
        with:
          ids: rest.ids
          updated_at[gt]: rest.updated_at[gt]
          updated_at[gte]: rest.updated_at[gte]
          updated_at[lt]: rest.updated_at[lt]
          updated_at[lte]: rest.updated_at[lte]
          linked_to_team_template: rest.linked_to_team_template
          search: rest.search
          tag_ids: rest.tag_ids
          tag: rest.tag
          filter_by_owner: rest.filter_by_owner
          group_id: rest.group_id
          include_cadence_templates: rest.include_cadence_templates
          include_archived_templates: rest.include_archived_templates
          cadence_id: rest.cadence_id
          sort_by: rest.sort_by
          sort_direction: rest.sort_direction
          per_page: rest.per_page
          page: rest.page
          include_paging_counts: rest.include_paging_counts
          limit_paging_counts: rest.limit_paging_counts
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Salesloft Create Personal Email Template
        call: salesloft-email-templates.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/email-templates/{id}
      name: email-templates-id
      description: REST surface for email_templates-id.
      operations:
      - method: PUT
        name: put
        description: Salesloft Update Personal Email Template
        call: salesloft-email-templates.put
        with:
          id: rest.id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: get
        description: Salesloft Fetch an Email Template
        call: salesloft-email-templates.get
        with:
          include_signature: rest.include_signature
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesloft-email-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesloft — Email Templates. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: salesloft-list-email-templates
      description: Salesloft List Email Templates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-email-templates.get
      with:
        ids: tools.ids
        updated_at[gt]: tools.updated_at[gt]
        updated_at[gte]: tools.updated_at[gte]
        updated_at[lt]: tools.updated_at[lt]
        updated_at[lte]: tools.updated_at[lte]
        linked_to_team_template: tools.linked_to_team_template
        search: tools.search
        tag_ids: tools.tag_ids
        tag: tools.tag
        filter_by_owner: tools.filter_by_owner
        group_id: tools.group_id
        include_cadence_templates: tools.include_cadence_templates
        include_archived_templates: tools.include_archived_templates
        cadence_id: tools.cadence_id
        sort_by: tools.sort_by
        sort_direction: tools.sort_direction
        per_page: tools.per_page
        page: tools.page
        include_paging_counts: tools.include_paging_counts
        limit_paging_counts: tools.limit_paging_counts
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-create-personal-email-template
      description: Salesloft Create Personal Email Template
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesloft-email-templates.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-update-personal-email-template
      description: Salesloft Update Personal Email Template
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: salesloft-email-templates.put
      with:
        id: tools.id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-fetch-email-template
      description: Salesloft Fetch an Email Template
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-email-templates.get
      with:
        include_signature: tools.include_signature
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
