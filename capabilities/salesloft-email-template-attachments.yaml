naftiko: 1.0.0-alpha2
info:
  label: Salesloft — Email Template Attachments
  description: 'Salesloft — Email Template Attachments. 1 operations. Lead operation: Salesloft List Email Template Attachments.
    Self-contained Naftiko capability covering one Salesloft business surface.'
  tags:
  - Salesloft
  - Email Template Attachments
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESLOFT_API_KEY: SALESLOFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesloft-email-template-attachments
    baseUri: http://{{salesloft_api_base_url}}
    description: Salesloft — Email Template Attachments business capability. Self-contained, no shared references.
    resources:
    - name: email_template_attachments
      path: /email_template_attachments
      operations:
      - name: get
        method: GET
        description: Salesloft List Email Template Attachments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ids
          in: query
          type: string
        - name: sort_by
          in: query
          type: string
        - name: sort_direction
          in: query
          type: string
        - name: per_page
          in: query
          type: string
        - name: page
          in: query
          type: string
        - name: include_paging_counts
          in: query
          type: string
        - name: limit_paging_counts
          in: query
          type: string
    authentication:
      type: bearer
      token: '{{env.SALESLOFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesloft-email-template-attachments-rest
    port: 8080
    description: REST adapter for Salesloft — Email Template Attachments. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/email-template-attachments
      name: email-template-attachments
      description: REST surface for email_template_attachments.
      operations:
      - method: GET
        name: get
        description: Salesloft List Email Template Attachments
        call: salesloft-email-template-attachments.get
        with:
          ids: rest.ids
          sort_by: rest.sort_by
          sort_direction: rest.sort_direction
          per_page: rest.per_page
          page: rest.page
          include_paging_counts: rest.include_paging_counts
          limit_paging_counts: rest.limit_paging_counts
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesloft-email-template-attachments-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesloft — Email Template Attachments. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: salesloft-list-email-template-attachments
      description: Salesloft List Email Template Attachments
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-email-template-attachments.get
      with:
        ids: tools.ids
        sort_by: tools.sort_by
        sort_direction: tools.sort_direction
        per_page: tools.per_page
        page: tools.page
        include_paging_counts: tools.include_paging_counts
        limit_paging_counts: tools.limit_paging_counts
      outputParameters:
      - type: object
        mapping: $.
