naftiko: 1.0.0-alpha2
info:
  label: Salesloft — Tasks
  description: 'Salesloft — Tasks. 4 operations. Lead operation: Salesloft List Tasks. Self-contained Naftiko capability covering
    one Salesloft business surface.'
  tags:
  - Salesloft
  - Tasks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESLOFT_API_KEY: SALESLOFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesloft-tasks
    baseUri: http://{{salesloft_api_base_url}}
    description: Salesloft — Tasks business capability. Self-contained, no shared references.
    resources:
    - name: tasks
      path: /tasks
      operations:
      - name: get
        method: GET
        description: Salesloft List Tasks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ids
          in: query
          type: string
        - name: user_id
          in: query
          type: string
        - name: person_id
          in: query
          type: string
        - name: account_id
          in: query
          type: string
        - name: current_state
          in: query
          type: string
        - name: task_type
          in: query
          type: string
        - name: time_interval_filter
          in: query
          type: string
        - name: completed_time_interval
          in: query
          type: string
        - name: idempotency_key
          in: query
          type: string
        - name: locale
          in: query
          type: string
        - name: source
          in: query
          type: string
        - name: updated_at[gt]
          in: query
          type: string
        - name: updated_at[gte]
          in: query
          type: string
        - name: updated_at[lt]
          in: query
          type: string
        - name: updated_at[lte]
          in: query
          type: string
        - name: completed_at[gt]
          in: query
          type: string
        - name: completed_at[gte]
          in: query
          type: string
        - name: completed_at[lt]
          in: query
          type: string
        - name: completed_at[lte]
          in: query
          type: string
        - name: include_counts_acted_on[gt]
          in: query
          type: string
        - name: include_counts_acted_on[gte]
          in: query
          type: string
        - name: include_counts_acted_on[lt]
          in: query
          type: string
        - name: include_counts_acted_on[lte]
          in: query
          type: string
        - name: sort_by
          in: query
          type: string
        - name: sort_direction
          in: query
          type: string
        - name: per_page
          in: query
          type: string
        - name: page
          in: query
          type: string
        - name: include_paging_counts
          in: query
          type: string
        - name: limit_paging_counts
          in: query
          type: string
      - name: post
        method: POST
        description: Salesloft Create a Task
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: tasks-265615502
      path: /tasks/265615502
      operations:
      - name: put
        method: PUT
        description: Salesloft Update a Task
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: tasks-id
      path: /tasks/{id}
      operations:
      - name: get
        method: GET
        description: Salesloft Fetch a Task
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESLOFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesloft-tasks-rest
    port: 8080
    description: REST adapter for Salesloft — Tasks. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/tasks
      name: tasks
      description: REST surface for tasks.
      operations:
      - method: GET
        name: get
        description: Salesloft List Tasks
        call: salesloft-tasks.get
        with:
          ids: rest.ids
          user_id: rest.user_id
          person_id: rest.person_id
          account_id: rest.account_id
          current_state: rest.current_state
          task_type: rest.task_type
          time_interval_filter: rest.time_interval_filter
          completed_time_interval: rest.completed_time_interval
          idempotency_key: rest.idempotency_key
          locale: rest.locale
          source: rest.source
          updated_at[gt]: rest.updated_at[gt]
          updated_at[gte]: rest.updated_at[gte]
          updated_at[lt]: rest.updated_at[lt]
          updated_at[lte]: rest.updated_at[lte]
          completed_at[gt]: rest.completed_at[gt]
          completed_at[gte]: rest.completed_at[gte]
          completed_at[lt]: rest.completed_at[lt]
          completed_at[lte]: rest.completed_at[lte]
          include_counts_acted_on[gt]: rest.include_counts_acted_on[gt]
          include_counts_acted_on[gte]: rest.include_counts_acted_on[gte]
          include_counts_acted_on[lt]: rest.include_counts_acted_on[lt]
          include_counts_acted_on[lte]: rest.include_counts_acted_on[lte]
          sort_by: rest.sort_by
          sort_direction: rest.sort_direction
          per_page: rest.per_page
          page: rest.page
          include_paging_counts: rest.include_paging_counts
          limit_paging_counts: rest.limit_paging_counts
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: Salesloft Create a Task
        call: salesloft-tasks.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/tasks/265615502
      name: tasks-265615502
      description: REST surface for tasks-265615502.
      operations:
      - method: PUT
        name: put
        description: Salesloft Update a Task
        call: salesloft-tasks.put
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/tasks/{id}
      name: tasks-id
      description: REST surface for tasks-id.
      operations:
      - method: GET
        name: get
        description: Salesloft Fetch a Task
        call: salesloft-tasks.get
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesloft-tasks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesloft — Tasks. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesloft-list-tasks
      description: Salesloft List Tasks
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-tasks.get
      with:
        ids: tools.ids
        user_id: tools.user_id
        person_id: tools.person_id
        account_id: tools.account_id
        current_state: tools.current_state
        task_type: tools.task_type
        time_interval_filter: tools.time_interval_filter
        completed_time_interval: tools.completed_time_interval
        idempotency_key: tools.idempotency_key
        locale: tools.locale
        source: tools.source
        updated_at[gt]: tools.updated_at[gt]
        updated_at[gte]: tools.updated_at[gte]
        updated_at[lt]: tools.updated_at[lt]
        updated_at[lte]: tools.updated_at[lte]
        completed_at[gt]: tools.completed_at[gt]
        completed_at[gte]: tools.completed_at[gte]
        completed_at[lt]: tools.completed_at[lt]
        completed_at[lte]: tools.completed_at[lte]
        include_counts_acted_on[gt]: tools.include_counts_acted_on[gt]
        include_counts_acted_on[gte]: tools.include_counts_acted_on[gte]
        include_counts_acted_on[lt]: tools.include_counts_acted_on[lt]
        include_counts_acted_on[lte]: tools.include_counts_acted_on[lte]
        sort_by: tools.sort_by
        sort_direction: tools.sort_direction
        per_page: tools.per_page
        page: tools.page
        include_paging_counts: tools.include_paging_counts
        limit_paging_counts: tools.limit_paging_counts
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-create-task
      description: Salesloft Create a Task
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesloft-tasks.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-update-task
      description: Salesloft Update a Task
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: salesloft-tasks.put
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesloft-fetch-task
      description: Salesloft Fetch a Task
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesloft-tasks.get
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
