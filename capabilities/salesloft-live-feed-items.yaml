naftiko: 1.0.0-alpha2
info:
  label: Salesloft — Live Feed Items
  description: 'Salesloft — Live Feed Items. 1 operations. Lead operation: Salesloft Create a Live Feed Item. Self-contained
    Naftiko capability covering one Salesloft business surface.'
  tags:
  - Salesloft
  - Live Feed Items
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESLOFT_API_KEY: SALESLOFT_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesloft-live-feed-items
    baseUri: http://{{salesloft_api_base_url}}
    description: Salesloft — Live Feed Items business capability. Self-contained, no shared references.
    resources:
    - name: third_party_live_feed_items
      path: /third_party_live_feed_items
      operations:
      - name: post
        method: POST
        description: Salesloft Create a Live Feed Item
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.SALESLOFT_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesloft-live-feed-items-rest
    port: 8080
    description: REST adapter for Salesloft — Live Feed Items. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/third-party-live-feed-items
      name: third-party-live-feed-items
      description: REST surface for third_party_live_feed_items.
      operations:
      - method: POST
        name: post
        description: Salesloft Create a Live Feed Item
        call: salesloft-live-feed-items.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesloft-live-feed-items-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesloft — Live Feed Items. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: salesloft-create-live-feed-item
      description: Salesloft Create a Live Feed Item
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesloft-live-feed-items.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
